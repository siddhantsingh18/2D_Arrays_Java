# 2D Arrays in Java

A **2D Array (Two-Dimensional Array)** in Java is an array of arrays. It is commonly used to represent data in a tabular format consisting of rows and columns, such as matrices, game boards, spreadsheets, and grids.
